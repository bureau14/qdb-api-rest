"""Arrow Flight SQL protocol: the gateway thesis measured end to end."""

TTFB_DEFINITION = "arrival of the first Arrow record batch"


def fetch(cfg, query, record_ttfb, telemetry):
    raise NotImplementedError("not enabled (docs/bench-plan.md)")
